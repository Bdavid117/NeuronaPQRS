Revisa el estado actual del vault Obsidian (Neurona/) del proyecto NeuronaPQRS.

Usando las herramientas del MCP `mcp-obsidian`:
1. Lista todas las notas con `obsidian_list_notes` (sin folder para ver el vault completo)
2. Lista los casos abiertos buscando en 20-Casos/ con `obsidian_list_notes` folder="20-Casos"
3. Busca notas con etiqueta `caso` usando `obsidian_list_by_tag` tag="caso"
4. Muestra un resumen: total de notas, notas por carpeta, y lista de casos existentes
5. Lee la nota `90-Sistema/Runbook-Escalamiento.md` si existe para ver el estado del sistema

Presenta un reporte conciso en español con la estructura del vault y los casos registrados.
